{{ form_start(formView,{'attr':{'id':'paymentBtnButtonMomoForm'}}) }}
    <div class="block">
        {{ form_row(formView._token) }}
    </div>
    <div class="form-group">
        <div class="col-md-8 col-md-offset-2">
            <label>Numéro momo</label>
            {{ form_widget(formView.number)}}
        </div>
    </div>
    {#<div class="form-group">
        <div class="col-md-8 col-md-offset-2">
            {{ form_widget(formView.holderName)}}
        </div>
    </div>#}
    {{ form_widget(formView.isYourAccount)}}
   {# <div class="checkbox-custom checkbox-success">
        <input type="checkbox"  id="isMyAccountJs">
        <label for="isMyAccountJs">Je suis le titulaire</label>
    </div>#}
    {{ form_row(formView.amount) }}
    <div class="button-wrapper">
        <button id="paymentBtnButtonMomo" type="submit"  style="
           border-radius: 0 !important;color:white !important;"  class="mb-xs mt-xs mr-xs btn btn-dark">
                <span class="icon icon-lg"><i class="fa fa-money"></i></span> Payer  <span id="recharge-montant-js" style="letter-spacing: 1px;">{{ amount ~ ' ' ~ currency }}</span>
        </button>
    </div>
{{ form_end(formView,{render_rest:false}) }}



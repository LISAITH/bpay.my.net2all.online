{{ form_start(formView,{'attr':{'id':'paymentBtnButtonMomoForm'}}) }}
    <div class="block">
        {{ form_errors(formView) }}
        {{ form_row(formView._token) }}
    </div>
    <div class="form-group  mt-sm">
        <label>Numéro Flooz (<span style="color:black;font-weight: bold;">Format: XX XX XX XX</span>)</label>
        <div class="input-group input-group-icon">
            {{ form_widget(formView.number)}}
        </div>
    </div>
    <div class="form-groupmb-sm mb-sm mt-sm" id="momo_pay_form_holderName_container">
        <label>Titulaire du compte Flooz</label>
        <div class="input-group input-group-icon">
            {{ form_widget(formView.holderName)}}
        </div>
    </div>
    <div class="checkbox-custom checkbox-success">
        <input type="checkbox"  id="isMyAccountJs">
        <label for="isMyAccountJs">Le compte vous m'appartient?</label>
    </div>
    {{ form_row(formView.amount) }}
    <button disabled id="paymentBtnButtonMomo" type="submit"  style="background-color: #ff5e17 !important;
      border: 0 !important;border-radius: 0 !important; color:white !important;"  class="btn btn-block btn-lg mt-lg">
        <span class="icon icon-lg"><i class="fa fa-money"></i></span> Recharger  <span id="recharge-montant-js" style="letter-spacing: 1px;">{{ amount ~ ' ' ~ currency }}</span>
    </button>

{{ form_end(formView,{render_rest:false}) }}


